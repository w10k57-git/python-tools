[project]
name = "engcalc"
version = "0.1.0"
description = "Engineering calculations library for unit conversions and formulas"
readme = "README.md"
requires-python = ">=3.11, <3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

